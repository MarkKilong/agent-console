# Agent harness the spawned runner uses: `claude` for the real SDK, `fake` for the
# scripted adapter (lets the UI run without Claude installed).
RUNNER_AGENT=fake

# Path to the Claude Code executable. Leave unset to resolve it from PATH.
# On Windows this must be claude.exe, not the .cmd shim.
# CLAUDE_BINARY=
